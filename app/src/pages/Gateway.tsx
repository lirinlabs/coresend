import { GatewayHeader } from "@/components/base/Header/GatewayHeader";

const Gateway = () => {
    return (
        <div>
            <GatewayHeader />
            Gateway Page
        </div>
    );
};

export default Gateway;
