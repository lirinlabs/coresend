echo "🧪 Running pre-push smoke tests..."

# Run Backend Tests
(cd backend && go test ./...) || { echo "❌ Backend tests failed"; exit 1; }

# Run Frontend Tests (Using Bun workspace filter)
bun run --filter frontend test || { echo "❌ Frontend tests failed"; exit 1; }

echo "🚀 Tests passed. Pushing..."